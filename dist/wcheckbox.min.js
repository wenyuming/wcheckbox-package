(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory();
	else if(typeof define === 'function' && define.amd)
		define([], factory);
	else {
		var a = factory();
		for(var i in a) (typeof exports === 'object' ? exports : root)[i] = a[i];
	}
})(typeof self !== 'undefined' ? self : this, function() {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/dist/";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 1);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});
//
//
//
//
//
//
//
//
//
//
//
//
//
//
//
//

exports.default = {
    props: ['value', 'options', 'bgc', 'direction', 'max', 'label', 'checked', 'relkey', 'encheckall'],
    data: function data() {
        return {
            isMax: false,
            initSelectedAll: false,
            // initOptions: this.options ? this.options : [],
            initSelected: this.value ? this.value : [],
            initBgc: this.bgc ? this.bgc : '#FFBF2F',
            initDirection: this.direction ? this.direction : 'horizontal',
            initMax: this.max ? Number(this.max) : this.options.length,
            initLabel: this.label ? this.label : 'label',
            initChecked: this.checked ? this.checked : 'checked',
            initRelKey: this.relkey ? this.relkey : 'id',
            initEnCheckAll: this.encheckall ? this.encheckall : false
        };
    },

    methods: {
        selectAll: function selectAll() {
            var _this = this;

            this.initSelectedAll = !this.initSelectedAll;
            if (this.initSelectedAll) {
                this.initOptions.forEach(function (e) {
                    if (_this.initSelected.indexOf(e[_this.initRelKey]) < 0) {
                        _this.initSelected.push(e[_this.initRelKey]);
                    }
                });
            } else {
                var length = this.initSelected.length;
                for (var i = 0; i < length; i++) {
                    console.log(i, 'i');
                    this.initSelected.pop();
                }
            }
            console.log(this.initSelected, 'this.initSelected');
            this.init();
            this.$emit('change', { val: this.initSelected });
        },
        select: function select(index) {
            var _this2 = this;

            console.log(index, 'index');
            if (!this.initOptions[index].checked && this.isMax) return;
            this.initOptions[index].checked = !this.initOptions[index].checked;
            var a = this.initOptions[index];
            this.initOptions.splice(index, 1, a);
            if (!this.initOptions[index].checked) {
                this.initSelected.forEach(function (e, index) {
                    if (e == a[_this2.initRelKey]) {
                        _this2.initSelected.splice(index, 1);
                    }
                });
            } else {
                this.initSelected.push(a[this.initRelKey]);
            }
            this.$emit('change', { val: this.initSelected, current: this.initOptions[index] });
        },
        init: function init() {
            var v = this;
            if (v.initOptions.length > 0) {
                v.initOptions.forEach(function (eg, index) {
                    eg.checked = false;
                    if (v.initSelected.length > 0) {
                        if (v.initSelected.indexOf(eg[v.relkey]) >= 0) {
                            eg.checked = true;
                            var a = eg;
                            v.initOptions.splice(index, 1, a);
                        }
                    }
                    // if (v.initSelected.length > 0) {
                    //  eg.checked = false
                    //  v.initSelected.forEach(item => {
                    //      // console.log(item, 'item')
                    //      // console.log(v.initRelKey, 'eg[v.initRelKey]')
                    //      if (item == eg[v.initRelKey]) {
                    //          eg.checked = true
                    //          let a = eg
                    //          v.initOptions.splice(index, 1, a)
                    //      }
                    //  })
                    // } else {
                    //  if (eg.checked) {
                    //      let a = eg
                    //      a.checked = false
                    //      v.initOptions.splice(index, 1, a)
                    //  }
                    //  eg.checked = false
                    // }
                });
            }
            console.log(v.initOptions, 'v.initOptions');
        }
    },
    computed: {
        initOptions: function initOptions() {
            var initOptions = this.options ? this.options : [];
            return initOptions;
        }
    },
    watch: {
        value: function value(val, oldVal) {
            this.initSelected = val;
            if (val.length != this.initOptions.length) {
                this.initSelectedAll = false;
            } else {
                this.initSelectedAll = true;
            }
            if (val.length >= this.initMax) {
                this.isMax = true;
            } else {
                this.isMax = false;
            }
            this.init();
        }
    },
    mounted: function mounted() {
        var v = this;
        v.initSelected = this.value;
        console.log(this.value, 'this.value');
        v.init();
        if (this.initSelected.length != this.initOptions.length) {
            this.initSelectedAll = false;
        } else {
            this.initSelectedAll = true;
        }
        if (this.initSelected.length >= this.initMax) {
            this.isMax = true;
        } else {
            this.isMax = false;
        }
    }
};

/***/ }),
/* 1 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _wcheckbox = __webpack_require__(2);

var _wcheckbox2 = _interopRequireDefault(_wcheckbox);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = _wcheckbox2.default; /**
                                        * Created by ty on 18/2/1.
                                        */

/***/ }),
/* 2 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__babel_loader_node_modules_vue_loader_lib_selector_type_script_index_0_wcheckbox_vue__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__babel_loader_node_modules_vue_loader_lib_selector_type_script_index_0_wcheckbox_vue___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0__babel_loader_node_modules_vue_loader_lib_selector_type_script_index_0_wcheckbox_vue__);
/* harmony namespace reexport (unknown) */ for(var __WEBPACK_IMPORT_KEY__ in __WEBPACK_IMPORTED_MODULE_0__babel_loader_node_modules_vue_loader_lib_selector_type_script_index_0_wcheckbox_vue__) if(__WEBPACK_IMPORT_KEY__ !== 'default') (function(key) { __webpack_require__.d(__webpack_exports__, key, function() { return __WEBPACK_IMPORTED_MODULE_0__babel_loader_node_modules_vue_loader_lib_selector_type_script_index_0_wcheckbox_vue__[key]; }) }(__WEBPACK_IMPORT_KEY__));
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__node_modules_vue_loader_lib_template_compiler_index_id_data_v_7b94069e_hasScoped_true_buble_transforms_node_modules_vue_loader_lib_selector_type_template_index_0_wcheckbox_vue__ = __webpack_require__(9);
var disposed = false
function injectStyle (ssrContext) {
  if (disposed) return
  __webpack_require__(3)
}
var normalizeComponent = __webpack_require__(8)
/* script */


/* template */

/* template functional */
var __vue_template_functional__ = false
/* styles */
var __vue_styles__ = injectStyle
/* scopeId */
var __vue_scopeId__ = "data-v-7b94069e"
/* moduleIdentifier (server only) */
var __vue_module_identifier__ = null
var Component = normalizeComponent(
  __WEBPACK_IMPORTED_MODULE_0__babel_loader_node_modules_vue_loader_lib_selector_type_script_index_0_wcheckbox_vue___default.a,
  __WEBPACK_IMPORTED_MODULE_1__node_modules_vue_loader_lib_template_compiler_index_id_data_v_7b94069e_hasScoped_true_buble_transforms_node_modules_vue_loader_lib_selector_type_template_index_0_wcheckbox_vue__["a" /* default */],
  __vue_template_functional__,
  __vue_styles__,
  __vue_scopeId__,
  __vue_module_identifier__
)
Component.options.__file = "src/wcheckbox.vue"

/* hot reload */
if (false) {(function () {
  var hotAPI = require("vue-hot-reload-api")
  hotAPI.install(require("vue"), false)
  if (!hotAPI.compatible) return
  module.hot.accept()
  if (!module.hot.data) {
    hotAPI.createRecord("data-v-7b94069e", Component.options)
  } else {
    hotAPI.reload("data-v-7b94069e", Component.options)
  }
  module.hot.dispose(function (data) {
    disposed = true
  })
})()}

/* harmony default export */ __webpack_exports__["default"] = (Component.exports);


/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

// style-loader: Adds some css to the DOM by adding a <style> tag

// load the styles
var content = __webpack_require__(4);
if(typeof content === 'string') content = [[module.i, content, '']];
if(content.locals) module.exports = content.locals;
// add the styles to the DOM
var update = __webpack_require__(6)("33305265", content, false, {});
// Hot Module Replacement
if(false) {
 // When the styles change, update the <style> tags
 if(!content.locals) {
   module.hot.accept("!!../node_modules/css-loader/index.js?sourceMap!../node_modules/vue-loader/lib/style-compiler/index.js?{\"vue\":true,\"id\":\"data-v-7b94069e\",\"scoped\":true,\"hasInlineConfig\":false}!../node_modules/vue-loader/lib/selector.js?type=styles&index=0!./wcheckbox.vue", function() {
     var newContent = require("!!../node_modules/css-loader/index.js?sourceMap!../node_modules/vue-loader/lib/style-compiler/index.js?{\"vue\":true,\"id\":\"data-v-7b94069e\",\"scoped\":true,\"hasInlineConfig\":false}!../node_modules/vue-loader/lib/selector.js?type=styles&index=0!./wcheckbox.vue");
     if(typeof newContent === 'string') newContent = [[module.id, newContent, '']];
     update(newContent);
   });
 }
 // When the module is disposed, remove the <style> tags
 module.hot.dispose(function() { update(); });
}

/***/ }),
/* 4 */
/***/ (function(module, exports, __webpack_require__) {

exports = module.exports = __webpack_require__(5)(true);
// imports


// module
exports.push([module.i, "\n.checkbox[data-v-7b94069e] {\n    margin-bottom: 0;\n}\n.checkbox-item-horizontal[data-v-7b94069e] {\n    font-size: 0;\n    display: inline-block;\n    line-height: 35px;\n}\n.checkbox-item-vertical[data-v-7b94069e] {\n    font-size: 0;\n    line-height: 35px;\n}\n.checked[data-v-7b94069e] {\n    display: inline-block;\n    width: 15px;\n    height: 15px;\n    box-sizing: border-box;\n    vertical-align: middle;\n    border: 1px solid #6F6F6F;\n    border-radius: 50%;\n    cursor: pointer;\n}\nspan.checked-active[data-v-7b94069e] {\n    background-color: #FFBF2F;\n    border:none !important;\n}\n.checked-active[data-v-7b94069e]:after {\n    font-size:14px;\n    content:\"\\2714\";\n    color: #FFF;\n    position: relative;\n    top: -10px;\n    left: 2px;\n}\n.checkbox-item-horizontal .checked-active[data-v-7b94069e]:after {\n    top: -10px;\n}\n.check-label[data-v-7b94069e] {\n    display: inline-block;\n    font-size: 14px;\n    vertical-align: middle;\n    color: #292929;\n    margin: 0 5px;\n    margin-right: 20px;\n    cursor: pointer;\n    -webkit-user-select:none;\n    -moz-user-select:none;\n    -ms-user-select:none;\n    user-select:none;\n}\n", "", {"version":3,"sources":["C:/Users/admin/Desktop/组件包/wcheckbox/src/src/wcheckbox.vue"],"names":[],"mappings":";AAqJA;IACA,iBAAA;CACA;AACA;IACA,aAAA;IACA,sBAAA;IACA,kBAAA;CACA;AACA;IACA,aAAA;IACA,kBAAA;CACA;AACA;IACA,sBAAA;IACA,YAAA;IACA,aAAA;IACA,uBAAA;IACA,uBAAA;IACA,0BAAA;IACA,mBAAA;IACA,gBAAA;CACA;AACA;IACA,0BAAA;IACA,uBAAA;CACA;AACA;IACA,eAAA;IACA,gBAAA;IACA,YAAA;IACA,mBAAA;IACA,WAAA;IACA,UAAA;CACA;AACA;IACA,WAAA;CACA;AACA;IACA,sBAAA;IACA,gBAAA;IACA,uBAAA;IACA,eAAA;IACA,cAAA;IACA,mBAAA;IACA,gBAAA;IACA,yBAAA;IACA,sBAAA;IACA,qBAAA;IACA,iBAAA;CACA","file":"wcheckbox.vue","sourcesContent":["<template>\n    <ul class=\"checkbox\">\n        <li :class=\"[initDirection == 'horizontal' ? 'checkbox-item-horizontal' : 'checkbox-item-vertical']\" v-if=\"initEnCheckAll\">\n            <span @click=\"selectAll()\" :class=\"['checked', initSelectedAll ? 'checked-active' : '']\"></span>\n            <div @click=\"selectAll()\" class=\"check-label\">\n                全选\n            </div>\n        </li>\n        <li :class=\"[initDirection == 'horizontal' ? 'checkbox-item-horizontal' : 'checkbox-item-vertical']\" v-for=\"(i, index) in initOptions\">\n            <span @click=\"select(index)\" :class=\"['checked', i.checked ? 'checked-active' : '']\"></span>\n            <div @click=\"select(index)\" class=\"check-label\">\n                {{initLabel ? i[initLabel] : i}}\n            </div>\n        </li>\n    </ul>\n</template>\n<script>\n    export default {\n        props: ['value','options','bgc','direction','max','label','checked','relkey','encheckall'],\n        data () {\n            return {\n                isMax:false,\n                initSelectedAll: false,\n                // initOptions: this.options ? this.options : [],\n                initSelected: this.value ? this.value : [],\n                initBgc: this.bgc ? this.bgc : '#FFBF2F',\n                initDirection: this.direction ? this.direction : 'horizontal',\n                initMax: this.max ? Number(this.max) : this.options.length,\n                initLabel: this.label ? this.label : 'label',\n                initChecked: this.checked ? this.checked : 'checked',\n                initRelKey: this.relkey ? this.relkey : 'id',\n                initEnCheckAll: this.encheckall ? this.encheckall : false\n            }\n        },\n        methods: {\n            selectAll () {\n                this.initSelectedAll = !this.initSelectedAll\n                if (this.initSelectedAll) {\n                    this.initOptions.forEach(e => {\n                        if (this.initSelected.indexOf(e[this.initRelKey]) < 0) {\n                            this.initSelected.push(e[this.initRelKey])\n                        }\n                    })\n                } else {\n                    let length = this.initSelected.length\n                    for (let i =0;i<length;i++) {\n                        console.log(i,'i')\n                        this.initSelected.pop()\n                    }\n                    \n                }\n                console.log(this.initSelected, 'this.initSelected')\n                this.init()\n                this.$emit('change', {val:this.initSelected})\n            },\n            select (index) {\n                console.log(index, 'index')\n                if (!this.initOptions[index].checked && this.isMax) return\n                this.initOptions[index].checked = !this.initOptions[index].checked\n                let a = this.initOptions[index]\n                this.initOptions.splice(index, 1, a)\n                if (!this.initOptions[index].checked) {\n                    this.initSelected.forEach((e, index) => {\n                        if (e == a[this.initRelKey]) {\n                            this.initSelected.splice(index,1)\n                        }\n                    })\n                } else {\n                    this.initSelected.push(a[this.initRelKey])\n                }\n                this.$emit('change', {val:this.initSelected, current: this.initOptions[index]})\n            },\n            init () {\n                let v = this\n                if (v.initOptions.length > 0) {\n                    v.initOptions.forEach((eg, index) => {\n                        eg.checked = false\n                        if (v.initSelected.length > 0) {\n                            if (v.initSelected.indexOf(eg[v.relkey]) >= 0) {\n                                eg.checked = true\n                                let a = eg\n                                v.initOptions.splice(index, 1, a)\n                            }\n                        }\n                        // if (v.initSelected.length > 0) {\n                        //  eg.checked = false\n                        //  v.initSelected.forEach(item => {\n                        //      // console.log(item, 'item')\n                        //      // console.log(v.initRelKey, 'eg[v.initRelKey]')\n                        //      if (item == eg[v.initRelKey]) {\n                        //          eg.checked = true\n                        //          let a = eg\n                        //          v.initOptions.splice(index, 1, a)\n                        //      }\n                        //  })\n                        // } else {\n                        //  if (eg.checked) {\n                        //      let a = eg\n                        //      a.checked = false\n                        //      v.initOptions.splice(index, 1, a)\n                        //  }\n                        //  eg.checked = false\n                        // }\n                    })\n                }\n                console.log(v.initOptions, 'v.initOptions')\n            }\n        },\n        computed: {\n            initOptions () {\n                let initOptions = this.options ? this.options : []\n                return initOptions\n            }\n        },\n        watch: {\n            value: function (val ,oldVal) {\n                this.initSelected = val\n                if (val.length != this.initOptions.length) {\n                    this.initSelectedAll = false\n                } else {\n                    this.initSelectedAll = true\n                }\n                if (val.length >= this.initMax) {\n                    this.isMax = true\n                } else {\n                    this.isMax = false\n                }\n                this.init()\n            }\n        },\n        mounted () {\n            let v = this\n            v.initSelected = this.value\n            console.log(this.value, 'this.value')\n            v.init()\n            if (this.initSelected.length != this.initOptions.length) {\n                this.initSelectedAll = false\n            } else {\n                this.initSelectedAll = true\n            }\n            if (this.initSelected.length >= this.initMax) {\n                this.isMax = true\n            } else {\n                this.isMax = false\n            }\n        }\n    }\n</script>\n<style scoped>\n    .checkbox {\n        margin-bottom: 0;\n    }\n    .checkbox-item-horizontal {\n        font-size: 0;\n        display: inline-block;\n        line-height: 35px;\n    }\n    .checkbox-item-vertical {\n        font-size: 0;\n        line-height: 35px;\n    }\n    .checked {\n        display: inline-block;\n        width: 15px;\n        height: 15px;\n        box-sizing: border-box;\n        vertical-align: middle;\n        border: 1px solid #6F6F6F;\n        border-radius: 50%;\n        cursor: pointer;\n    }\n    span.checked-active {\n        background-color: #FFBF2F;\n        border:none !important;\n    }\n    .checked-active:after {\n        font-size:14px;\n        content:\"\\2714\";\n        color: #FFF;\n        position: relative;\n        top: -10px;\n        left: 2px;\n    }\n    .checkbox-item-horizontal .checked-active:after {\n        top: -10px;\n    }\n    .check-label {\n        display: inline-block;\n        font-size: 14px;\n        vertical-align: middle;\n        color: #292929;\n        margin: 0 5px;\n        margin-right: 20px;\n        cursor: pointer;\n        -webkit-user-select:none;\n        -moz-user-select:none;\n        -ms-user-select:none;\n        user-select:none;\n    }\n</style>"],"sourceRoot":""}]);

// exports


/***/ }),
/* 5 */
/***/ (function(module, exports) {

/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Tobias Koppers @sokra
*/
// css base code, injected by the css-loader
module.exports = function(useSourceMap) {
	var list = [];

	// return the list of modules as css string
	list.toString = function toString() {
		return this.map(function (item) {
			var content = cssWithMappingToString(item, useSourceMap);
			if(item[2]) {
				return "@media " + item[2] + "{" + content + "}";
			} else {
				return content;
			}
		}).join("");
	};

	// import a list of modules into the list
	list.i = function(modules, mediaQuery) {
		if(typeof modules === "string")
			modules = [[null, modules, ""]];
		var alreadyImportedModules = {};
		for(var i = 0; i < this.length; i++) {
			var id = this[i][0];
			if(typeof id === "number")
				alreadyImportedModules[id] = true;
		}
		for(i = 0; i < modules.length; i++) {
			var item = modules[i];
			// skip already imported module
			// this implementation is not 100% perfect for weird media query combinations
			//  when a module is imported multiple times with different media queries.
			//  I hope this will never occur (Hey this way we have smaller bundles)
			if(typeof item[0] !== "number" || !alreadyImportedModules[item[0]]) {
				if(mediaQuery && !item[2]) {
					item[2] = mediaQuery;
				} else if(mediaQuery) {
					item[2] = "(" + item[2] + ") and (" + mediaQuery + ")";
				}
				list.push(item);
			}
		}
	};
	return list;
};

function cssWithMappingToString(item, useSourceMap) {
	var content = item[1] || '';
	var cssMapping = item[3];
	if (!cssMapping) {
		return content;
	}

	if (useSourceMap && typeof btoa === 'function') {
		var sourceMapping = toComment(cssMapping);
		var sourceURLs = cssMapping.sources.map(function (source) {
			return '/*# sourceURL=' + cssMapping.sourceRoot + source + ' */'
		});

		return [content].concat(sourceURLs).concat([sourceMapping]).join('\n');
	}

	return [content].join('\n');
}

// Adapted from convert-source-map (MIT)
function toComment(sourceMap) {
	// eslint-disable-next-line no-undef
	var base64 = btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap))));
	var data = 'sourceMappingURL=data:application/json;charset=utf-8;base64,' + base64;

	return '/*# ' + data + ' */';
}


/***/ }),
/* 6 */
/***/ (function(module, exports, __webpack_require__) {

/*
  MIT License http://www.opensource.org/licenses/mit-license.php
  Author Tobias Koppers @sokra
  Modified by Evan You @yyx990803
*/

var hasDocument = typeof document !== 'undefined'

if (typeof DEBUG !== 'undefined' && DEBUG) {
  if (!hasDocument) {
    throw new Error(
    'vue-style-loader cannot be used in a non-browser environment. ' +
    "Use { target: 'node' } in your Webpack config to indicate a server-rendering environment."
  ) }
}

var listToStyles = __webpack_require__(7)

/*
type StyleObject = {
  id: number;
  parts: Array<StyleObjectPart>
}

type StyleObjectPart = {
  css: string;
  media: string;
  sourceMap: ?string
}
*/

var stylesInDom = {/*
  [id: number]: {
    id: number,
    refs: number,
    parts: Array<(obj?: StyleObjectPart) => void>
  }
*/}

var head = hasDocument && (document.head || document.getElementsByTagName('head')[0])
var singletonElement = null
var singletonCounter = 0
var isProduction = false
var noop = function () {}
var options = null
var ssrIdKey = 'data-vue-ssr-id'

// Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
// tags it will allow on a page
var isOldIE = typeof navigator !== 'undefined' && /msie [6-9]\b/.test(navigator.userAgent.toLowerCase())

module.exports = function (parentId, list, _isProduction, _options) {
  isProduction = _isProduction

  options = _options || {}

  var styles = listToStyles(parentId, list)
  addStylesToDom(styles)

  return function update (newList) {
    var mayRemove = []
    for (var i = 0; i < styles.length; i++) {
      var item = styles[i]
      var domStyle = stylesInDom[item.id]
      domStyle.refs--
      mayRemove.push(domStyle)
    }
    if (newList) {
      styles = listToStyles(parentId, newList)
      addStylesToDom(styles)
    } else {
      styles = []
    }
    for (var i = 0; i < mayRemove.length; i++) {
      var domStyle = mayRemove[i]
      if (domStyle.refs === 0) {
        for (var j = 0; j < domStyle.parts.length; j++) {
          domStyle.parts[j]()
        }
        delete stylesInDom[domStyle.id]
      }
    }
  }
}

function addStylesToDom (styles /* Array<StyleObject> */) {
  for (var i = 0; i < styles.length; i++) {
    var item = styles[i]
    var domStyle = stylesInDom[item.id]
    if (domStyle) {
      domStyle.refs++
      for (var j = 0; j < domStyle.parts.length; j++) {
        domStyle.parts[j](item.parts[j])
      }
      for (; j < item.parts.length; j++) {
        domStyle.parts.push(addStyle(item.parts[j]))
      }
      if (domStyle.parts.length > item.parts.length) {
        domStyle.parts.length = item.parts.length
      }
    } else {
      var parts = []
      for (var j = 0; j < item.parts.length; j++) {
        parts.push(addStyle(item.parts[j]))
      }
      stylesInDom[item.id] = { id: item.id, refs: 1, parts: parts }
    }
  }
}

function createStyleElement () {
  var styleElement = document.createElement('style')
  styleElement.type = 'text/css'
  head.appendChild(styleElement)
  return styleElement
}

function addStyle (obj /* StyleObjectPart */) {
  var update, remove
  var styleElement = document.querySelector('style[' + ssrIdKey + '~="' + obj.id + '"]')

  if (styleElement) {
    if (isProduction) {
      // has SSR styles and in production mode.
      // simply do nothing.
      return noop
    } else {
      // has SSR styles but in dev mode.
      // for some reason Chrome can't handle source map in server-rendered
      // style tags - source maps in <style> only works if the style tag is
      // created and inserted dynamically. So we remove the server rendered
      // styles and inject new ones.
      styleElement.parentNode.removeChild(styleElement)
    }
  }

  if (isOldIE) {
    // use singleton mode for IE9.
    var styleIndex = singletonCounter++
    styleElement = singletonElement || (singletonElement = createStyleElement())
    update = applyToSingletonTag.bind(null, styleElement, styleIndex, false)
    remove = applyToSingletonTag.bind(null, styleElement, styleIndex, true)
  } else {
    // use multi-style-tag mode in all other cases
    styleElement = createStyleElement()
    update = applyToTag.bind(null, styleElement)
    remove = function () {
      styleElement.parentNode.removeChild(styleElement)
    }
  }

  update(obj)

  return function updateStyle (newObj /* StyleObjectPart */) {
    if (newObj) {
      if (newObj.css === obj.css &&
          newObj.media === obj.media &&
          newObj.sourceMap === obj.sourceMap) {
        return
      }
      update(obj = newObj)
    } else {
      remove()
    }
  }
}

var replaceText = (function () {
  var textStore = []

  return function (index, replacement) {
    textStore[index] = replacement
    return textStore.filter(Boolean).join('\n')
  }
})()

function applyToSingletonTag (styleElement, index, remove, obj) {
  var css = remove ? '' : obj.css

  if (styleElement.styleSheet) {
    styleElement.styleSheet.cssText = replaceText(index, css)
  } else {
    var cssNode = document.createTextNode(css)
    var childNodes = styleElement.childNodes
    if (childNodes[index]) styleElement.removeChild(childNodes[index])
    if (childNodes.length) {
      styleElement.insertBefore(cssNode, childNodes[index])
    } else {
      styleElement.appendChild(cssNode)
    }
  }
}

function applyToTag (styleElement, obj) {
  var css = obj.css
  var media = obj.media
  var sourceMap = obj.sourceMap

  if (media) {
    styleElement.setAttribute('media', media)
  }
  if (options.ssrId) {
    styleElement.setAttribute(ssrIdKey, obj.id)
  }

  if (sourceMap) {
    // https://developer.chrome.com/devtools/docs/javascript-debugging
    // this makes source maps inside style tags work properly in Chrome
    css += '\n/*# sourceURL=' + sourceMap.sources[0] + ' */'
    // http://stackoverflow.com/a/26603875
    css += '\n/*# sourceMappingURL=data:application/json;base64,' + btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))) + ' */'
  }

  if (styleElement.styleSheet) {
    styleElement.styleSheet.cssText = css
  } else {
    while (styleElement.firstChild) {
      styleElement.removeChild(styleElement.firstChild)
    }
    styleElement.appendChild(document.createTextNode(css))
  }
}


/***/ }),
/* 7 */
/***/ (function(module, exports) {

/**
 * Translates the list format produced by css-loader into something
 * easier to manipulate.
 */
module.exports = function listToStyles (parentId, list) {
  var styles = []
  var newStyles = {}
  for (var i = 0; i < list.length; i++) {
    var item = list[i]
    var id = item[0]
    var css = item[1]
    var media = item[2]
    var sourceMap = item[3]
    var part = {
      id: parentId + ':' + i,
      css: css,
      media: media,
      sourceMap: sourceMap
    }
    if (!newStyles[id]) {
      styles.push(newStyles[id] = { id: id, parts: [part] })
    } else {
      newStyles[id].parts.push(part)
    }
  }
  return styles
}


/***/ }),
/* 8 */
/***/ (function(module, exports) {

/* globals __VUE_SSR_CONTEXT__ */

// IMPORTANT: Do NOT use ES2015 features in this file.
// This module is a runtime utility for cleaner component module output and will
// be included in the final webpack user bundle.

module.exports = function normalizeComponent (
  rawScriptExports,
  compiledTemplate,
  functionalTemplate,
  injectStyles,
  scopeId,
  moduleIdentifier /* server only */
) {
  var esModule
  var scriptExports = rawScriptExports = rawScriptExports || {}

  // ES6 modules interop
  var type = typeof rawScriptExports.default
  if (type === 'object' || type === 'function') {
    esModule = rawScriptExports
    scriptExports = rawScriptExports.default
  }

  // Vue.extend constructor export interop
  var options = typeof scriptExports === 'function'
    ? scriptExports.options
    : scriptExports

  // render functions
  if (compiledTemplate) {
    options.render = compiledTemplate.render
    options.staticRenderFns = compiledTemplate.staticRenderFns
    options._compiled = true
  }

  // functional template
  if (functionalTemplate) {
    options.functional = true
  }

  // scopedId
  if (scopeId) {
    options._scopeId = scopeId
  }

  var hook
  if (moduleIdentifier) { // server build
    hook = function (context) {
      // 2.3 injection
      context =
        context || // cached call
        (this.$vnode && this.$vnode.ssrContext) || // stateful
        (this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext) // functional
      // 2.2 with runInNewContext: true
      if (!context && typeof __VUE_SSR_CONTEXT__ !== 'undefined') {
        context = __VUE_SSR_CONTEXT__
      }
      // inject component styles
      if (injectStyles) {
        injectStyles.call(this, context)
      }
      // register component module identifier for async chunk inferrence
      if (context && context._registeredComponents) {
        context._registeredComponents.add(moduleIdentifier)
      }
    }
    // used by ssr in case component is cached and beforeCreate
    // never gets called
    options._ssrRegister = hook
  } else if (injectStyles) {
    hook = injectStyles
  }

  if (hook) {
    var functional = options.functional
    var existing = functional
      ? options.render
      : options.beforeCreate

    if (!functional) {
      // inject component registration as beforeCreate hook
      options.beforeCreate = existing
        ? [].concat(existing, hook)
        : [hook]
    } else {
      // for template-only hot-reload because in that case the render fn doesn't
      // go through the normalizer
      options._injectStyles = hook
      // register for functioal component in vue file
      options.render = function renderWithStyleInjection (h, context) {
        hook.call(context)
        return existing(h, context)
      }
    }
  }

  return {
    esModule: esModule,
    exports: scriptExports,
    options: options
  }
}


/***/ }),
/* 9 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
var render = function() {
  var _vm = this
  var _h = _vm.$createElement
  var _c = _vm._self._c || _h
  return _c(
    "ul",
    { staticClass: "checkbox" },
    [
      _vm.initEnCheckAll
        ? _c(
            "li",
            {
              class: [
                _vm.initDirection == "horizontal"
                  ? "checkbox-item-horizontal"
                  : "checkbox-item-vertical"
              ]
            },
            [
              _c("span", {
                class: ["checked", _vm.initSelectedAll ? "checked-active" : ""],
                on: {
                  click: function($event) {
                    _vm.selectAll()
                  }
                }
              }),
              _vm._v(" "),
              _c(
                "div",
                {
                  staticClass: "check-label",
                  on: {
                    click: function($event) {
                      _vm.selectAll()
                    }
                  }
                },
                [_vm._v("\n            全选\n        ")]
              )
            ]
          )
        : _vm._e(),
      _vm._v(" "),
      _vm._l(_vm.initOptions, function(i, index) {
        return _c(
          "li",
          {
            class: [
              _vm.initDirection == "horizontal"
                ? "checkbox-item-horizontal"
                : "checkbox-item-vertical"
            ]
          },
          [
            _c("span", {
              class: ["checked", i.checked ? "checked-active" : ""],
              on: {
                click: function($event) {
                  _vm.select(index)
                }
              }
            }),
            _vm._v(" "),
            _c(
              "div",
              {
                staticClass: "check-label",
                on: {
                  click: function($event) {
                    _vm.select(index)
                  }
                }
              },
              [
                _vm._v(
                  "\n            " +
                    _vm._s(_vm.initLabel ? i[_vm.initLabel] : i) +
                    "\n        "
                )
              ]
            )
          ]
        )
      })
    ],
    2
  )
}
var staticRenderFns = []
render._withStripped = true
var esExports = { render: render, staticRenderFns: staticRenderFns }
/* harmony default export */ __webpack_exports__["a"] = (esExports);
if (false) {
  module.hot.accept()
  if (module.hot.data) {
    require("vue-hot-reload-api")      .rerender("data-v-7b94069e", esExports)
  }
}

/***/ })
/******/ ]);
});
//# sourceMappingURL=wcheckbox.min.js.map